# Note

Boards were drawn in Python (Pillow) and encoded with ffmpeg, libx264, yuv420p, authored 12 fps. No Blender file was opened. The product blend was not saved. Film A is a cue-sheet HUD, not a re-render of the yaw twin.

`render_boards.py` rebuilds every card. The arm on film A is a drawing whose swell and bend follow `p_norm`. It is not a measured silhouette. Driver names and shape keys are ASSUMPTION proposals from the HUD brief. The live blend was not opened. Soft stays idle on that file.

12 fps is the film clock. It is not a measured camera, detector, or machine rate. It is not τ_el or τ_vent.

Raw frames under `raw/` are local rebuild scratch and are gitignored.

Catalog dollar rows that appear inside copied integrate papers are not drawn on the films. Films say paper only / Critical OPEN.
